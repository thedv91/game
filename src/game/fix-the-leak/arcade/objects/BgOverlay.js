/*
 * BgOverlay
 */

export default class BgOverlay extends Phaser.TileSprite {

	constructor(game, x, y, w, h) {
		super(game, x, y, w, h, 'black');

		// TODO:
		//   1. Edit constructor parameters accordingly.
		//   2. Adjust object properties.
	}

}
